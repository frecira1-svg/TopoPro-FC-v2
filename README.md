# TopoPro FC

## V2 - Publicar trabajo

Esta versión incorpora metadatos profesionales para las publicaciones: tipo de trabajo, ubicación y etiquetas.

### Base de datos
Ejecuta `npx prisma migrate deploy` en `backend` antes de iniciar la aplicación en un entorno con la base de datos actualizada.
